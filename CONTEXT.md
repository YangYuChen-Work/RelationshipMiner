# CONTEXT — ai-graph

> 领域术语表。不含实现细节。

## 核心概念

- **数据记录（Data Record）** — 图中的节点。对应数据库表中一行数据。
- **关系（Relationship）** — 图中的边。连接两条数据记录，表示它们之间存在关联。
- **关系强度（Relationship Strength）** — 关系的分层属性，分为强关系和弱关系，用户可分别筛选。
  - **强关系（Strong Relationship）** — 确定性的关联，如外键约束、字段值精确匹配。
  - **弱关系（Weak Relationship）** — 推断性的关联，如语义相似、AI 推测的隐含关系。
- **全量分析（Full Analysis）** — 对用户选中范围内的每一条记录都参与关系计算，不抽样、不先归纳规则再套用。
- **数据范围（Data Scope）** — 用户在前端通过勾选表与字段指定的分析边界。
- **类名（Class Name）** — 每条数据记录来源的 Java 类全限定名，记录在表的一个字段中。是发现继承关系的独立维度，不影响其他类型关系的计算。每次分析必须包含。系统通过约定命名自动识别该字段（`class_name` / `className` / `class`）。
- **多重关系（Multi-Label Edge）** — 两条记录之间如存在多种关系类型，合并为一条边，标签拼接展示所有关系类型。
- **继承关系（Inheritance Relationship）** — 基于 Java 类层级发现的关系。数据库中对继承的表达方式可能有三种：
  - **同表继承** — 同一张表内通过字段标记父子类关系。
  - **跨表继承** — 不同表对应不同类层级，通过外键关联。
  - **元数据表继承** — 独立的关系映射表描述类之间的继承链。

## 关系发现方式

- **Schema 层发现** — 基于外键约束等数据库元数据发现的关系。
- **数据层发现** — 基于字段值精确相等匹配发现的关系。
- **语义层发现** — AI 推断字段含义相似但名称不同（如 `user_email` ≈ `contact_email`）的关系。

## AI 的角色

- **AI 决策者（AI Decision Maker）** — AI 负责判定字段之间的语义匹配关系，以及为模糊匹配选择合适的相似度算法。AI 不做逐行推理。
- **系统执行者（System Executor）** — 系统根据 AI 的决策，对全量数据执行关系计算。包括精确值比较、外键追踪、以及 AI 选定的相似度算法运算。

## 图谱交互

- **全局图谱（Global Graph）** — 力导向布局的完整关系图谱，支持缩放与拖拽。
- **节点悬停高亮（Hover Highlight）** — 鼠标悬停节点时，高亮该节点及其关联节点，关联深度不限。
- **关系标签（Relationship Label）** — 每条边上标注文字，说明该关系的具体含义。
- **局部图谱导航（Local Graph Navigation）** — 单击节点高亮关联，双击节点打开详情面板。
- **节点详情面板（Node Detail Panel）** — 双击节点后展示该记录的所有字段原始值及关联节点列表。
- **图谱快照（Graph Snapshot）** — MVP 1.0 中每次分析结果为一次性快照，不与源数据库实时同步。可保存为项目供后续查阅。
- **关系强度筛选（Strength Filter）** — 连续滑块控制置信度阈值（0.0–1.0），边的显示/隐藏由阈值自动划分，强/弱边界由阈值动态决定。

## 用户操作流程（MVP 1.0）

1. 连接数据库 → 浏览表列表
2. 勾选表（≤10 张）→ 查看字段列表
3. 勾选字段（`class_name` 必须包含，其余通常选 2-4 个，允许全选）
4. 点击"开始分析"
5. 等待（≤3 分钟，WebSocket 实时进度）→ 图谱呈现
6. 交互探索（悬停高亮、单击关联、双击详情、关系筛选）、保存/导出 JSON

> **延后至后续版本：**
> - 参数调整重新生成
> - 节点/边标注批注
> - 手动创建/删除关系
> - 表内关系发现（同一张表内的记录与记录之间的关联）
> - 边数量渲染上限保护（超过阈值时仅渲染 Top-N 置信度边）
> - AI 相似度算法选项池扩展（编辑距离、Jaccard、Jaro-Winkler、embedding 等）
> - NULL 值匹配策略（当前不匹配任何值包括另一个 NULL）
> - 继承关系发现的确定性规则优化（前缀树/层级解析）

## 分析规模约束

- 最多 10 张表
- 每张表最多约 1000 行
- 字段通常 2-4 个，可全选
- `class_name` 字段必须参与分析
- 分析上限 3 分钟

## 字段语义匹配

- **上下文感知（Context-Aware）** — AI 基于表名+字段名的组合判定字段语义，而非仅看字段名。例如 `users.name` 和 `products.name` 不应匹配。

## 技术约束

- **数据库** — MySQL（MVP 1.0）
- **AI 引擎** — DeepSeek API
- **架构** — Web 前后端分离：React + TypeScript + D3.js + Tailwind + Zustand（前端），Python FastAPI + SQLAlchemy + NumPy（后端）
- **持久化** — JSON 文件导出/导入，图谱即文件
- **连接配置** — 后端 `.env` 文件
- **进度反馈** — WebSocket 实时推送分析阶段进度
- **节点视觉** — 颜色按表名区分，大小按关系度数，图标按 class_name 类型
- **界面语言** — 全中文（关系标签、界面文本）
- **空状态** — 未发现关系时显示提示信息 + 仅节点图谱
- **JSON 导出** — 完整快照：图谱数据（节点+边+坐标）+ 原始数据（所有字段值）+ 分析配置（表/字段选择、AI 决策结果）+ 布局状态（x/y 坐标）
- **异常处理** — 友好错误提示 + 操作建议：数据库连接失败、AI 服务不可用、分析超时均给出具体排查方向
