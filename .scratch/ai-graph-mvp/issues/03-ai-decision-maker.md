# 03 — AI 决策者

**What to build:** 将 DeepSeek 集成进分析流水线阶段 3。AI 接收表名+字段名+字段类型+少量样本值，返回字段语义匹配对及每对推荐的相似度算法。上下文感知——基于表名+字段名组合区分语义（`users.name` ≠ `products.name`）。

**Blocked by:** 02 — 分析流水线 + 实时进度

**Status:** ready-for-agent

- [ ] DeepSeek API 集成模块，通过 OpenAI 兼容 SDK 调用
- [ ] AI 决策者纯函数接口：输入表 schema 列表 → 输出字段匹配决策列表（匹配对 + 推荐算法 + 置信度）
- [ ] 上下文感知：AI 基于表名+字段名组合判定语义，不盲目匹配同名字段
- [ ] 相似度算法推荐：针对不同值类型推荐合适算法（字符串用编辑距离、数值用差值比例等）
- [ ] AI 决策插入流水线阶段 3，输出传递至阶段 4 用于相似度计算
- [ ] 语义层关系与强关系的置信度正确区分（语义层 < 1.0，确定性层 = 1.0）
- [ ] AI 决策者模块单元测试覆盖（mock DeepSeek API，验证输入/输出契约）
- [ ] 缺少 class_name 字段的表正常参与 FK 和精确匹配分析，仅跳过继承发现
