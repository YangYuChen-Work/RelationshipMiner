# 04 — 图谱可视化 + 全部交互

**What to build:** 分析完成后，在力导向图谱中渲染节点和边。节点颜色按表区分、大小按度数区分、图标按 class_name 类型区分。边标注合并关系标签。完整交互：悬停高亮（不限深度）、单击局部导航、双击详情面板、置信度滑块筛选。

**Blocked by:** 02 — 分析流水线 + 实时进度

**Status:** ready-for-agent

- [ ] GraphCanvas 组件：D3 forceSimulation 力导向布局，支持缩放与拖拽
- [ ] 节点视觉编码：颜色按来源表名区分（每表一色）、半径按度数（关系数）、图标按 class_name 类型
- [ ] 边渲染：带方向箭头，标签展示合并后的关系类型文字（如"外键关联 + 值相等(email)"）
- [ ] 悬停节点高亮该节点及其全部关联节点（不限深度），其余节点/边淡出
- [ ] 单击节点以该节点为中心展示局部图谱（仅显示直接关联节点，或切换视图中心）
- [ ] 双击节点打开 NodeDetailPanel 右侧抽屉，展示该记录所有字段原始值及关联节点列表
- [ ] StrengthFilter 连续滑块（0.0–1.0），拖拽实时调节边显示/隐藏的置信度阈值
- [ ] 孤立节点（无任何关系）正常显示，不被隐藏或移动
- [ ] 图谱数据从 Zustand store 读取，悬停/选中节点 ID、置信度阈值、详情面板开关均由 store 管理
- [ ] 组件渲染测试：TableSelector、FieldSelector、StrengthFilter 交互行为
- [ ] 前端集成测试：完整用户流程（mock 后端 API 和 WebSocket）
